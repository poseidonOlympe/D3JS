/** 
        const areaRadial = d3.areaRadial()
        .angle(angle)
        .startAngle(startAngle)
        .endAngle(endAngle)
        .radius(radius)
        .innerRadius(innerRadius)
        .outerRadius(outerRadius)
        .radius(radius)
        .defined(defined)
        .curve(curve)
        .context(context)
        //.lineStartAngle(lineStartAngle)
        //.lineInnerRadius(lineInnerRadius)
        //.lineEndAngle(lineEndAngle)
        //.lineOuterRadius(lineOuterRadius)

    setD3AreaRadial(areaRadial);
*/